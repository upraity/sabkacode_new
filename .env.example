# No environment variables are required yet.
# This project currently reads content from local data files in lib/data/.
#
# When you add a real database later, this is where connection info goes, e.g.:
# DATABASE_URL="postgresql://user:password@host:5432/sabkacode"
